# maven via docker
alias mvn="docker run -it --rm -v "$PWD":/usr/src/mymaven -v "$HOME/.m2":/root/.m2 -w /usr/src/mymaven maven mvn"
# Add cookiecutter command from docker image
alias cookiecutter="docker run --rm -it -v $(pwd):/cookiecutter ckaserer/cookiecutter "
